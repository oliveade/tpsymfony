{% extends 'base.html.twig' %}

{% block title %}Générer un Menu{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center text-primary mb-4">🍽️ Génération Automatique de Menu</h1>

        <div class="card shadow-sm p-4 mb-4">
            {{ form_start(form) }}
                <div class="mb-3">
                    {{ form_row(form.cuisine) }}
                </div>
                <div class="mb-3">
                    {{ form_row(form.nbPlats) }}
                </div>
                <button type="submit" class="btn btn-success w-100">🔄 Générer le Menu</button>
            {{ form_end(form) }}
        </div>

        {% if menu %}
            <h2 class="text-center text-secondary mt-4">🍷 Menu Généré 🍷</h2>
            <div class="row mt-3">
                {% for plat in menu %}
                    <div class="col-md-4 mb-4">
                        <div class="card border-0 shadow-lg">
                            <div class="card-body">
                                <h5 class="card-title text-center">🍽️ {{ plat }}</h5>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endif %}
    </div>
{% endblock %}
