{% extends 'base.html.twig' %}

{% block title %}Générer un Menu{% endblock %}

{% block body %}
<div class="max-w-3xl mx-auto mt-10">
    <h1 class="text-3xl font-bold text-center mb-6">🍽️ Génération Automatique de Menu</h1>

    <div class="bg-white shadow-md rounded-lg p-6">
        {{ form_start(form, {'attr': {'class': 'space-y-4'}}) }}
            {{ form_widget(form, {'attr': {'class': 'w-full border-gray-300 rounded-md shadow-sm'}}) }}
            <button type="submit" class="w-full bg-blue-600 text-white py-2 rounded-md hover:bg-blue-700 transition">
                Générer
            </button>
        {{ form_end(form) }}
    </div>

    {% if menu %}
    
        <div class="bg-white shadow-lg rounded-lg mt-6 p-6">
            <h2 class="text-2xl font-semibold text-gray-800">{{ menu.name }}</h2>
            <p class="text-gray-600 mb-4">{{ menu.description }}</p>

            <div class="overflow-x-auto">
                <table class="min-w-full border-collapse border border-gray-300">
                    <thead class="bg-gray-100">
                        <tr>
                            <th class="border border-gray-300 px-4 py-2 text-left">Nom</th>
                            <th class="border border-gray-300 px-4 py-2 text-left">Description</th>
                            <th class="border border-gray-300 px-4 py-2 text-left">Prix (€)</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for dish in menu.dishes %}
                            <tr class="hover:bg-gray-50"> 
                                <td class="border border-gray-300 px-4 py-2 font-semibold">{{ dish.name }}</td>
                                <td class="border border-gray-300 px-4 py-2">{{ dish.description }}</td>
                                <td class="border border-gray-300 px-4 py-2 text-right font-bold">{{ dish.price }}</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    {% endif %}
</div>
{% endblock %}
