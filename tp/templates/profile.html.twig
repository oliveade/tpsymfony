<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Mon Profil{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">

<header class="bg-cyan-600 text-white py-4">
    <div class="container mx-auto flex justify-between items-center">
        <h1 class="text-2xl font-bold">Bienvenue, {{ app.user.firstname }}</h1>
        <nav>
            <ul class="flex space-x-4">
                <li><a href="{{ path('app_home') }}" class="hover:underline">Accueil</a></li>
                <li><a href="{{ path('app_logout') }}" class="hover:underline">Déconnexion</a></li>
            </ul>
        </nav>
    </div>
</header>

<main class="container mx-auto p-6">
    <div class="bg-white rounded-lg shadow-md p-6">
        <h2 class="text-3xl font-bold mb-4 text-gray-700">Mon Profil</h2>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="bg-gray-50 p-4 rounded-lg shadow">
                <h3 class="text-xl font-semibold mb-3">Informations personnelles</h3>
                <p><strong>Prénom :</strong> {{ app.user.firstname }}</p>
                <p><strong>Nom :</strong> {{ app.user.lastname }}</p>
                <p><strong>Email :</strong> {{ app.user.email }}</p>
            </div>

            <div class="bg-gray-50 p-4 rounded-lg shadow">
                <h3 class="text-xl font-semibold mb-3">Actions</h3>
                <ul class="space-y-2">
                    <li>
                        <a href="" 
                        class="block bg-blue-500 text-white px-4 py-2 rounded-lg text-center hover:bg-blue-600">
                        Modifier mes informations
                     </a>
                     
                    </li>
                
                    <li>
                        <a href="" 
                           class="block bg-red-500 text-white px-4 py-2 rounded-lg text-center hover:bg-red-600"
                           onclick="return confirm('Êtes-vous sûr de vouloir supprimer votre compte ?');">
                           Supprimer mon compte
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</main>
</body>
</html>
